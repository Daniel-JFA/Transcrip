import { Routes } from '@angular/router';

import { TranscriptionComponent } from './transcription.component';

export const TRANSCRIPTION_ROUTES: Routes = [
  {
    path: '',
    component: TranscriptionComponent,
  },
];
